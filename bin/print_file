#!/usr/bin/env ruby

require 'cupsffi'

if ARGV.length != 2
  puts "Usage: #{$0} <file_path> <printer_name>"
  exit 1
end

file_path = ARGV[0]
printer_name = ARGV[1]

CupsPrinter.new(printer_name).print_file(file_path)
